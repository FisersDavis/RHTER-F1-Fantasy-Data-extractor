.env
.env.local
*.key
*.pem
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.venv/
Lib/
Scripts/
Include/
pyvenv.cfg

# Local data (not served)
data/

# Node
node_modules/
dist/

# Tailwind standalone CLI binary (downloaded locally, not committed)
tailwindcss.exe

# Generated CSS (built from src/input.css, not committed)
docs/style.css

# Git worktrees
.worktrees/

# GitNexus local index and HuggingFace embedding cache — must never be committed (audit Phase 2)
.gitnexus/
.gitnexusignore
undefined/

# optional local tooling output dirs — add to your global gitignore if needed
