# Legacy Python Tests

These tests cover the legacy Python extraction pipeline kept for reference and fallback maintenance.

The primary product workflow is now browser-only (upload -> extract -> review -> analysis in the web app).

Run these tests only when intentionally modifying legacy Python code.
